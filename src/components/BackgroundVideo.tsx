import React from 'react';

// This is just a placeholder component since the video is handled in HTML
const BackgroundVideo: React.FC = () => {
  return null;
};

export default BackgroundVideo; 